{
  "name": "hello-circle",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "private-circle encrypt --page-id hello-circle --content content/index-plaintext.html --hashes hashes --out dist",
    "encrypt": "npm run build"
  },
  "devDependencies": {
    "@kummahiih/private-circle": "^0.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
